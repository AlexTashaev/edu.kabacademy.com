# Лекция 1 «Суть науки каббала» — схемы 1–3

Курс 238, секция «🟢 ОСНОВА» → **lesson 1960 / cmid 13413**, страницы
17879 (глава 1), 17880 (глава 2), 17881 (глава 3).
URL: https://edu.kabacademy.com/mod/lesson/view.php?id=13413

В тексте лекции стояли три бежевых плейсхолдера «Схема N. … заказ дизайнеру».
24.09.2026 они заменены на схемы, свёрстанные в дизайн-системе курса
(палитра темы kabacademy: #3C5D90 / #52B0D8 / #A42BB9, подложка #F4F9FD, рамка #D9E8F2,
Montserrat даёт тема). Плейсхолдеры «Клип» (3 шт.) не тронуты — ждут нарезки видео.

| Схема | Где | Что | Источник у преподавателей |
|---|---|---|---|
| 1 | глава 1, после «Два слова, из которых состоит всё» | Творец (сила отдачи, свет) наполняет творение (желание получать, сосуд-кли) | «04 Строение мироздания», слайд «Творец и Творение» |
| 2 | глава 2, «Путь снизу вверх» | Нисхождение по 4 ступеням без участия желания → наш мир → подъём по тем же ступеням своим усилием | «04», слайд «Стадии развития творения»; «11 Зарождение творения», стадии 0–4 |
| 3 | глава 2, «Что двигает человека вверх» | Лестница желаний: телесные → богатство → почёт и власть → знание → вопрос о смысле; эпохи истории сбоку | «02 Развитие желаний», слайд «Пирамида желаний» + слайды эпох |

Папка преподавателей: Google Drive `1ejeWkSzCOo9b9hE_nC3sI3ye7FP_-ShX` (pptx по лекциям).
Презентация «01_Вводное занятие + Суть науки каббала» (15 МБ) через коннектор Drive не
скачивается (лимит 10 МБ) — те же стандартные схемы МАК взяты из 02/04/11/12.

## Как устроено

- `schemas/schema<N>.html` — фрагмент схемы. **Только inline-стили, без SVG и без классов
  темы**: переживает TinyMCE, purifier и мобильное приложение; стрелки — CSS-треугольники,
  сосуд — `border-radius`. Одинарных кавычек в HTML быть не должно (SQL-литерал).
  Класс `kab-figure` — просто хук на будущее, стилей у него нет.
- `orig/page-<id>.html` — контент страниц до вставки (CRLF, как лежало в БД). Клиент mysql
  при чтении файла выбрасывает CR даже внутри литералов, поэтому `pages/` и prod — с LF.
- `build.py` — подставляет фрагменты вместо плейсхолдеров → `pages/`, `upd_lesson1960.sql`,
  `rollback_lesson1960.sql`; `--preview-dir DIR` пишет `preview.html` (в DIR нужны
  `fonts.css` + `fonts/Montserrat-*.woff2`, взять с `https://edu.kabacademy.com/kab/fonts/`).
  Узкий экран: `preview.html?w=330`.

## Применить / откатить

```
scp .claude/course238/lecture1/upd_lesson1960.sql web-18:/tmp/
ssh web-18 '/tmp/kab_moodle_sqlfile.sh /tmp/upd_lesson1960.sql'
```

Бэкап страниц — таблица `_kab_bk20260924_lesson1960` (создаётся один раз, `IF NOT EXISTS`,
повторный прогон её не перезапишет). Откат: `rollback_lesson1960.sql` тем же раннером.
Кэш чистить не нужно: страницы урока читаются из БД на каждом показе.

build.py также заменяет слово «Рав» на «Михаэль Лайтман» — правило заказчика (24.09.2026)
для всех текстов курса, в лекции 1 это три подписи к клипам.

Параллельно с схемами клипы в те же страницы вставляла другая сессия
(`.claude/lesson1_clips/`, ветка claude/lecture-1-video-matching-322c33): её снимки
`before/after` устарели после схем — пересобирать страницы только из свежего снятия БД.

Копия лекции 1 в боевом курсе 236 (lesson 1961 / cmid 13444, секция «🟢 ОСНОВА») сделана
24.09 до клипов и схем — там ещё плейсхолдеры; после финализации 238 копию обновить заново.

Править схему: менять `schemas/schema<N>.html` → `python build.py` → применить SQL.
Через редактор Moodle тоже можно («</>» исходный код), но тогда `orig/` устареет —
перед следующей сборкой снять контент заново (`SELECT TO_BASE64(contents) …`).
